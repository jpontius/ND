Printer Notes
==

```bash
lpstat -p -d	#	 Get list of printers installed.
lpq			# Show printer queue
cancel -a LaserJet600	# Cancel all jobs for "printer"
lpq			# Show printer queue
cupsenable LaserJet600	# Re-enable printer
lpq			# Show printer queue
```

